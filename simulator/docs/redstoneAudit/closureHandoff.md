# Claude 阻塞执行后的收尾（2026-09-09）

实际 Claude CLI Opus 单会话从 2026-09-08 23:22 至 09-09 05:38 运行约 6 小时 16 分钟。
退出原因是 API 无响应；CLI 错误报告显示它内部经历了 3 分钟及 10 分钟等待。
本次没有证据确认额度耗尽，编排器未再次启动 Claude，也未等待额度重置或消费重置额度。
原生会话持久化开启；逐行 stream JSON、断点及原生会话的 SHA-256 校验副本均在本地忽略目录保留。

## 已交付与未完成

Claude 在 fork 关闭 #1–#11、#13，共 12 个 issue，修复和证据已推送 `codex/redstone-audit`。
#10 是有范围的调查结论，R13 没有动态反例，也没有排除所有竞争历史；#13 是覆盖清单和拒绝占位器件的门禁，不是全部未实现方块的功能交付。

#12（漏斗/投掷器实体反馈链）、#14（进一步特性验证）、汇总 #15 保持打开。
区块生命周期只交付设计，没有实现和原版动态验证；严格 vanilla-only 专服尚未复跑。
跟踪目前仅记录邻居/形状更新坐标及刻标记，不能区分同坐标的更新类型、通知来源，也不记录全部计划刻、方块事件和 RNG 消耗。
跟踪开关的两次参考采样原点不同，仍需同环境对照。源码字段引用检索也不构成不同功能开关环境整体等价的证明。

## Codex 仅收尾最近的随机差分工作

断点文件落后于实际执行：远程 #14 已有 Claude 的 16 轮通过报告。本次没有重复启动 Claude，按实际捕获复核：

- 两种种子 71/99，各 8 轮，合计 18726 条命令、3117 个观察位置（各场景之和）：原版捕获在记录的绝对坐标重放 C++，16/16 一致。
- Claude 最后的 `fullAudit15` 共 38 组原版捕获，重新运行 C++ 检查器，38/38 一致。本项是重放原始捕获，不冒称 Codex 又启动了 38 次原版。
- 修正缩减工具后，使用新种子 20260909 运行 2 轮新原版捕获，合计 2319 条命令、373 个观察位置，2/2 一致。窗口仍为 12 gt，25 个小格子/轮，不能外推为长周期/跨区块/实体链路兼容。
- Release CTest 3/3；新增缩减器失败路径检查 4/4。机器轨迹等既有回归由 CTest 和上述捕获重放验证，本次没有重跑浏览器端到端或 sanitizer。

捕获 SHA-256、原点、环境和比较结果见 [closureReplayResults.json](closureReplayResults.json)。本次 C++ 基线仍为 `a565697`，Codex 没有扩大范围改动新的器件规则。

## 缩减器补足的条件

1. 检查器非零退出却返回 match、未知状态等不允许视为通过。
2. 缩减必须保留首差异坐标、时刻、字段和双方观察值；候选末次捕获重新比较并明确 `reducedWitnessConfirmed`，不因之前某一步失败就声称最终文件仍失败。
3. 每轮使用独立缩减子目录，避免下一轮覆盖上轮过程证据；保存原始/候选捕获哈希、环境和各自原点。
4. 明确这是有步数预算的命令删减，不能称全局最小，预算耗尽时也没有局部最小保证。格子隔离只是候选步骤，未证明所有电路绝对互不影响。
5. GameTest 每次重选原点。缩减候选只证明自身记录坐标下的差异，不能称它保留了原始坐标的竞争行为。
   **2026-09-09 续跑已补上**：默认改用严格 vanilla-only 服务器在**同一个固定原点**上完成整轮的
   全部捕获，缩减候选因此在原始绝对坐标上确认（报告里的 `reducedAtOriginalCoordinates`）；
   `--gameTest` 保留旧路径。缩减仍是有预算的贪心删除，不是最小性证明。
6. 生成器从合法注册表状态取样并直接调用 setBlock，包含无支撑方块和陈旧状态；不保证每段历史都能由普通玩家搭建。它能审计状态处理分支，典型玩家机器仍需单独验证。

复跑：`python simulator/tests/referenceFuzzTests.py`；完整原版工具的前提和调用见 [auditReport.md](auditReport.md)。
