# simulator 实施记录

## 授权和目标

用户已批准开始实现 C++ 原生仿真 + 浏览器 Three.js 方案。代码统一位于 simulator/，使用驼峰命名；每个经过验证的大更新 Git 提交，说明不超过 20 字。

默认先在当前 macOS 开发机验证，工程编辑操作为主，目标规模遵循设计。TNT 复制、完整流体、矿车计算机与生物 AI 机器不在本模块范围内。

## 当前状态

- [x] 初始化 Git，记录约定及目录边界。
- [x] S0：固定参考版本与工具链，建立 C++ 核心/测试基础。
- [x] S1：三维搭建、基础供电、单步、保存和示波器。
- [x] S2：更新顺序、侦测器、活塞与动态拓扑的基础实现。
- [ ] S3：容器、扩展器件和环境刺激。
- [ ] S4：性能、回放、完整验证与工作流交付。

2026-09-07：完成 [VMCB 1.0](vmcbFormat.md) 设计及原生读写接入，工程菜单默认 `.vmcb`，兼容旧 JSON 导入。分区布局不构造逐方块 JSON；器件/运行表分页，独立压缩、CRC 和规则/快照 ABI 检查。HTTP 文件传输包含会话校验、阶段进度、取消及临时文件清理，成功导入可撤销，失败保留世界与历史。Release 与 ASan/UBSan 的 83 项核心测试、11 组 VMCB 测试通过；独立 Python 读取器、文件协议回归、前端构建与浏览器导入/下载实操通过。已测 10 万、约 105 万、200 万方块以及稀疏/库存布局的原生文件往返，详见 [测量与边界](vmcbFiles.md)。未知扩展段目前明确拒绝；百万方块的浏览器刷新、完整大型动态快照和持续模糊测试仍需专项验收。

2026-09-07 文件工作流修正：用户要求 JSON 仅用于旧工程导入，已移除 UI 和 HTTP 文件任务的 JSON 导出。bootstrap 增加文件接口版本，前端在调用前检查，启动器识别旧后台，避免新版页面对旧服务发送 exportFile。文件传输回归加入拒绝 JSON 导出，前端加入旧服务能力回归。

2026-09-07 视角操作：三维工作台获得焦点后支持方向键平移，沿用 OrbitControls 的屏幕平面平移与阻尼；点击工作台聚焦，输入框和下拉框保留原有按键行为，销毁视口时移除键盘监听。同步操作提示与使用说明。前端生产构建、8 项现有前端测试通过；浏览器实操四向平移、搜索框光标移动和编辑层数值调整通过，控制台无错误。

2026-09-07 视角快捷键调整：按用户要求将平移改为 WASD，键盘平移步长由 7 提高到 21 像素，速度为原来的 3 倍；单步快捷键改为 F，避免 S 同时推进仿真。禁用组合键触发的键盘旋转，保留 Ctrl/Cmd+S 保存。同步工作台提示、帮助与使用说明。前端生产构建与 8 项现有前端测试通过；独立本地服务的浏览器实操验证 WASD 四向平移、S 不推进游戏刻、F 单步以及输入框正常输入，控制台无错误。

只有具有实际证据的行为才标记“已验证”；实现代码与原版差分覆盖分别记录。

## 已完成的核心基础

- C++20 / CMake，16³ 稀疏分块、连续方块状态表、确定性邻居更新栈和计划刻队列。
- 26.2 服务端 SHA-1 校验，导出 1,196 种原版方块状态定义、方向供电和支撑面数据；数据覆盖不代表全部器件实现。
- 当前可执行器件：结构方块、红石块、拉杆、按钮、红石粉、火把、中继器、比较器、侦测器、红石灯、铜灯、活塞/黏性活塞、黏液/蜂蜜黏连、门/活板门/栅栏门，以及环境输入驱动的压力板、阳光探测器、避雷针、讲台、标靶和部分状态读数器件。普通箱、陷阱箱、铜箱、木桶和漏斗支持真实库存与传输。具体环境边界见 [environmentInputs.md](environmentInputs.md)。其他器件暂时拒绝放置。
- 原生探针、同刻有序边沿、触发暂停、VCD、工程文件与包含队列/内部状态的运行快照、原子导入。
- Release 与 ASAN/UBSAN 构建的 83 项测试通过。包含全部原版状态的属性往返、负坐标、时序与快照测试，二十一组真实 26.2 GameTest 状态差分（25 刻 / 47 点、29 刻 / 32 点、17 刻 / 14 点、65 刻 / 25 点、195 刻 / 5 点、45 刻 / 93 点、25 刻 / 8 点、70 刻 / 121 点、101 刻 / 16 点、31 刻 / 19 点、53 刻 / 26 点、25 刻 / 132 点、57 刻 / 11 点、73 刻 / 26 点、161 刻 / 13 点、155 刻 / 31 点、100 刻 / 34 点、123 刻 / 34 点、151 刻 / 68 点、1,456 刻 / 11 点、111 刻 / 26 点），原版调度器专项，以及阳光探测器 11,536 个数值对照。
- 差分只是这些场景的证据，不代表更新顺序、所有位置/朝向或全部器件完整兼容。

## 当前继续工作

已打通原生 HTTP/WebSocket、本地会话校验、二进制增量与确认、Three.js 分块实例渲染、逐层放置/移除/操作/检查、原生快照撤销、工程导入/导出、示波器及触发。浏览器实操拉杆—中继器—红石灯闭环通过，入门场景实测约 60 FPS。Python 标准库协议测试覆盖原生交互、延迟、VCD、错误原子回滚和撤销/重做。

活塞增加方块事件/方块实体阶段、12 方块解析、准连接、黏连与吐块、移动方块状态、运动快照和原生驱动的动态显示。新增“活塞实验”样例。中止的邻居更新不能清掉暂停标志后继续执行，必须恢复有效状态。

接下来继续 S3 器件扩展；S4 仍需要大型电路、随机与调度专项、区域编辑、持久恢复和性能验收。已完成基础阶段不代表模块一完整完成。

S3 第一批完成环境面板与“环境实验”样例。比较器会在隔着实体方块的模拟读数改变时收到通知，门的更新携带来源方块。运行快照按坐标排序内部状态，消除哈希表顺序造成的文件差异。浏览器实操压力板—铁门、讲台—实体方块—比较器通过。

S3 库存批次开放普通箱、陷阱箱和木桶，支持真实槽位、双箱、查看人数与库存快照。浏览器放置箱子、写入 64 个石头并开箱的闭环通过。漏斗传输已实现，加工仍在实现中，详见 [inventoryModel.md](inventoryModel.md)。

S3 漏斗批次增加先推后拉、8 gt 工作冷却、空接收方时序、锁定与局部唤醒。方块实体使用持久注册顺序，快照保存并核验冷却和队列。原版逐槽对照和 5,000 个空漏斗休眠验证通过；普通容器提取失败的可观察重试保留。掉落物吸入、加工与其他扩展器件仍未完成。

漏斗浏览器实操通过：装入两件石头、单步推出一件，五槽库存与读数同步更新；完整 HTTP/WebSocket 测试覆盖 13 个二进制增量帧及漏斗快照续跑，前端构建通过。

性能批次：针对原生采样中的区块查询热点添加 8 项指针缓存，11 万活跃基准中位耗时从 3.393 s 降至 2.208 s，更新数量保持一致。详见 [性能记录](performance.md)；大型混合电路等验收仍在进行。

火把修复：熄灭历史改为世界级记录，拆除和同坐标重放置不会清除烧毁计数，运行快照保留该历史。恢复 26.2 火把每次状态放入世界时的邻居通知，并遵守跳过 onPlace 的标志。新增原版 195 刻差分和拆除期间快照测试通过。

S3 铁轨批次完成四类轨道的连接、坡道、岔路切换、供电和探测读数。矿车接触使用显式列表，容器矿车保留真实库存；完整运动与碰撞不在首版。新增 45 刻 / 93 点原版差分、快照测试及浏览器操作验证，接口测试通过。详见 [铁轨说明](rails.md)。

调度批次修正原版“先收集、再执行”的计划刻语义，分别处理未来排期与本刻待执行查询，保留零延迟跨刻、每刻 65,536 项上限及跨区块积压顺序。快照可恢复本刻批次中途的暂停。36 项原生测试和 14 帧协议测试通过，详见 [调度说明](tickScheduling.md)。

S3 绊线批次完成绊线钩连接、强弱供电、接触输入、10 gt 复查、普通断线与剪刀解除触发。补充环境接触阶段及快照，修复形状更新破坏方块时漏通知的问题。39 项原生测试、八组 GameTest 与 16 帧协议测试通过。浏览器实操触发、10 gt 释放、11 gt 重新触及和剪断通过；窄窗口保留完整检查面板，绊线按整格选取体积点选。具体范围见 [绊线说明](tripwire.md)。

S4 采样批次：协议 3 将场景与波形一起确认，以最慢窗口的接收游标保护未确认边沿；软容量暂停后可续跑，超出单次更新安全余量则明确中止。修复运行快照静默裁剪历史的问题，浏览器按探针索引和时间窗二分查询绘制。42 项原生测试、4 项前端测试、完整协议流程与多窗口慢接收测试通过。浏览器实操单步、清空波形及重新同步通过，控制台无错误。详见 [采样传输](traceTransport.md)。

S3 按钮补全：木按钮支持箭矢接触与 30 gt 复查，区分弹起和按下检测形状；石质按钮忽略箭矢。保留浅接触在同刻先释放再按下的边沿与接触阶段快照。新增九组中的按钮差分（101 刻 / 16 点）、边沿与快照测试以及环境输入面板。45 项原生测试和完整协议测试通过。浏览器实操在 30 gt 先暂停于输出 0，再执行接触事件变为 15，游戏刻保持不变，控制台无错误。

S4 运行控制修复：不限速改为只执行实际事件，稳定后停在最后事件刻；状态栏显示连续运行的计划事件/s，排除手动单步和空闲跳跃。46 项原生测试、4 项前端测试、运行控制与既有协议/慢接收测试通过。浏览器实操有限按钮脉冲准确停在 60 gt，显示稳定原因与 0 事件/s，控制台无错误。固定工作负载的性能验收仍独立进行，不能用界面统计代替。

随机源基础：增加原版 48 位随机运算、位模式和状态恢复，6,000 次原版调用逐位对照通过；构建禁用隐式浮点乘加融合。当前共 47 项原生测试通过。随机源尚未接入世界、投掷器或声音/游戏事件，相关器件并未因此标记完成。详见 [随机源说明](randomSource.md)。

S3 投掷器批次：九槽库存、随机选槽、4 gt 投放、目标容器插入与外部物品初始输出；外部动作暂停、带版本确认、导出及快照恢复。原版 31 刻 / 19 点、600 次选槽和 36 组实际弹出的浮点位模式对照通过；Release 与 ASAN/UBSAN 共 51 项原生测试、4 项前端测试、投掷器及既有协议回归通过。世界随机源已接入工程，完整声音/游戏事件随机消耗仍待补全，见 [投掷器说明](droppers.md)。

投掷器浏览器实操：选择九槽库存并写入 64 个石头，按钮触发后在 4 gt 抛出一件、库存变为 63；反馈卡片显示位置/速度，确认后剩余按钮事件在 20 gt 执行。修复新增反馈卡片被检查面板滚动位置遮住的问题；控制台无错误。

S3 标靶修正：环境输入改为命中面和格内坐标，按原版世界坐标双精度公式计算 1–15 强度，拒绝旧的直接 `value` 输入；补上带电标靶新放置清零及遗留计划刻例外。4,272 个数值样本、53 刻 / 26 点原版差分和协议测试通过，原生 Release/ASAN/UBSAN 共 54 项通过。浏览器中心命中 15、近边缘 2、点选坐标 8，20/8 gt 脉冲与重复命中保持通过，控制台无错误。

S3 铜箱批次：开放八种铜箱、跨变体配对、涂蜡/氧化程度协调和转换保留库存；修复双箱东西朝向显示。64 种配对的原版玩家放置、25 刻 / 132 点差分通过；Release 与 ASAN/UBSAN 共 56 项原生测试、68 帧服务回归通过。浏览器验证合并成斑驳铜箱、54 槽保留石头及同步开盖，控制台无错误。自然氧化与铜傀儡 AI 不在当前实验环境内，详见 [铜箱说明](copperChests.md)。

S3 雕纹书架批次：六槽书籍、最后操作槽位、书籍过滤、漏斗抽取前检查和局部休眠唤醒。原版 57 刻 / 11 点差分，Release 与 ASAN/UBSAN 共 59 项原生测试、74 帧服务回归及投掷器回归通过。浏览器验证第六槽写入并清空后仍输出 6，控制台无错误。内部游戏事件在后续振动批次接入，详见 [书架说明](bookshelves.md)。

S3 陶罐批次：单槽库存、物品容量、比较器、漏斗与六向投掷；按原版保留成功传输通知，并让无副作用的失败抽取休眠。73 刻 / 26 点差分通过，Release 与 ASAN/UBSAN 共 61 项原生测试、80 帧服务回归及投掷器回归通过。浏览器验证满罐/清空/撤销读数。玩家插入和击碎反馈仍待补齐，详见 [陶罐说明](decoratedPots.md)。

检查面板刷新修复：库存和环境面板共用串行合并刷新器，请求期间的变化保留为后续刷新；切换选择后忽略旧回包，失败请求不锁死刷新状态。新增受控延迟/突发更新/取消/失败测试，7 项前端测试和生产构建通过。

S3 振动批次：普通/校频感测体的候选、传播、遮挡、共振、供电与冷却；独立频率探针及在途快照。接入按钮、拉杆、压力板、门、箱子/木桶、书架、绊线和活塞事件，保留相关音高随机消耗。新增原版 161 刻 / 13 点与 155 刻 / 31 点，Release/ASAN/UBSAN 共 66 项、7 项前端测试和完整协议回归通过。浏览器验证距离 3 格脚步在 3 gt 抵达，强度 10、频率 1，控制台无错误。尚缺直接踩踏强制路径和其他器件事件来源，详见 [振动说明](vibrations.md)。

S3 音符盒批次：材质/头颅乐器、上方遮挡、调音与上升沿、方块事件去重、演奏振动和检查记录。导出全部基础乐器映射与 27 种乐器、25 个音高；原版 100 刻 / 34 点和 112 次隔离随机对照通过。Release/ASAN/UBSAN 共 70 项、协议回归和前端生产构建通过。浏览器验证小号音符 1 在 1 gt 执行，4 gt 感测体强度 8、频率 10，控制台无错误。未实现浏览器声音播放及下落方块物理，详见 [音符盒说明](notes.md)。

S3 钟批次：供电、命中面/高度过滤、单双墙转换、频率 11 振动、摆动结束排期及恢复。123 刻 / 34 点原版差分和 2,688 次命中边界对照通过；Release/ASAN/UBSAN 共 74 项、协议回归及前端构建通过。浏览器手工放置、无效命中过滤、有效敲响与 50 gt 停止通过，控制台无错误。同时修正音符盒放置旁已有电源时额外触发的问题，详见 [钟的说明](bells.md)。

S3 唱片机批次：22 张默认唱片、播放与曲终供电、比较器、库存过滤和动态执行注册。原版 151 刻 / 68 点、1,456 刻 / 11 点和 4,265 个完整播放随机样本通过；Release/ASAN/UBSAN 共 79 项、7 项前端测试、服务协议与浏览器实操通过。修复空闲唤醒时间的浏览器 JSON 精度，以及环境/库存面板标识冲突。活动播放仍逐刻计时，玩家取盘掉落反馈与音频未完成，详见 [唱片机说明](jukeboxes.md)。

S3 堆肥桶批次：115 种材料概率、成熟排期、六方向访问过滤和临时容器副作用。4,176 次原版随机插入及 111 刻 / 26 点差分通过；Release/ASAN/UBSAN 共 82 项、服务协议与浏览器实操通过。失败投料的随机推进可撤销；第七层在 20 gt 成熟，空闲不扫描。手动取骨粉掉落反馈未完成，详见 [堆肥桶说明](composters.md)。

S4 查询与测量批次：写入复用区块缓存，按需查询支撑，空邻居回调提前返回，并将只读状态表对齐到 64 字节。与 `f6446b0` 交错五组测量，11 万方块 / 5 万红石器件的中位耗时下降 6.07%，为 149.03 gt/s；64 个实际切换探针的吞吐损失 1.72%，不含浏览器传输和绘图。22 场景、9,830 条完整状态记录逐字节一致；83 项 Release/ASAN/UBSAN、协议、背压和浏览器单步通过。原现场完整恢复，测量方法和未达成的验收项见 [性能记录](performance.md)。
