# simulator 实施记录

## 授权和目标

用户已批准开始实现 C++ 原生仿真 + 浏览器 Three.js 方案。代码统一位于 simulator/，使用驼峰命名；每个经过验证的大更新 Git 提交，说明不超过 20 字。

默认先在当前 macOS 开发机验证，工程编辑操作为主，目标规模遵循设计。TNT 复制、完整流体、矿车计算机与生物 AI 机器不在本模块范围内。

## 当前状态

- [x] 初始化 Git，记录约定及目录边界。
- [x] S0：固定参考版本与工具链，建立 C++ 核心/测试基础。
- [x] S1：三维搭建、基础供电、单步、保存和示波器。
- [x] S2：更新顺序、侦测器、活塞与动态拓扑的基础实现。
- [ ] S3：容器、扩展器件和环境刺激。
- [ ] S4：性能、回放、完整验证与工作流交付。

只有具有实际证据的行为才标记“已验证”；实现代码与原版差分覆盖分别记录。

## 已完成的核心基础

- C++20 / CMake，16³ 稀疏分块、连续方块状态表、确定性邻居更新栈和计划刻队列。
- 26.2 服务端 SHA-1 校验，导出 1,196 种原版方块状态定义、方向供电和支撑面数据；数据覆盖不代表全部器件实现。
- 当前可执行器件：结构方块、红石块、拉杆、按钮、红石粉、火把、中继器、比较器、侦测器、红石灯、铜灯、活塞/黏性活塞、黏液/蜂蜜黏连与标靶刺激。其他器件暂时拒绝放置。
- 原生探针、同刻有序边沿、触发暂停、VCD、工程文件与包含队列/内部状态的运行快照、原子导入。
- Release 构建的 15 项测试通过。包含全部原版状态的属性往返、负坐标、时序与快照测试，以及真实 26.2 GameTest 生成的 25 刻 / 47 个观测点状态差分。
- 差分只是这些场景的证据，不代表更新顺序、所有位置/朝向或全部器件完整兼容。

## 当前继续工作

已打通原生 HTTP/WebSocket、本地会话校验、二进制增量与确认、Three.js 分块实例渲染、逐层放置/移除/操作/检查、原生快照撤销、工程导入/导出、示波器及触发。浏览器实操拉杆—中继器—红石灯闭环通过，入门场景实测约 60 FPS。Python 标准库协议测试覆盖原生交互、延迟、VCD、错误原子回滚和撤销/重做。

活塞增加方块事件/方块实体阶段、12 方块解析、准连接、黏连与吐块、移动方块状态、运动快照和原生驱动的动态显示。新增“活塞实验”样例。中止的邻居更新不能清掉暂停标志后继续执行，必须恢复有效状态。

接下来继续 S3 器件扩展；S4 仍需要大型电路、随机与调度专项、区域编辑、持久恢复和性能验收。已完成基础阶段不代表模块一完整完成。
