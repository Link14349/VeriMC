# 在 WSL 里执行的脚本必须保持 LF。仓库设了 core.autocrlf=true，
# 检出时会把它们改成 CRLF，于是 `wsl bash foo.sh` 报「command not found」——
# 行尾的回车被当成命令名的一部分。
# 同样的原因，参考捕获的逐文件哈希只有按 LF 归一之后才可复核：
# 之前两份证据文件对同一个 fixture 记过两个不同的 sha256，差别就来自行尾。
*.sh text eol=lf
*.py text eol=lf
