# VeriMC 项目协作约定

## 当前阶段与授权边界

- 用户已明确说“开始做吧”，当前进入模块一实施阶段。v0.3 的 C++ 原生后端 + 浏览器 Three.js 方案获准实施，常规构建、依赖、测试、启动本地服务与 Git 提交均在授权范围内。
- 用户于 2026-09-06 要求同时开展第二子项目 VeriMC 语言设计，全部内容放在 `verimc/`。先提交主要设计思路供审阅，用户拍板后再展开完整语法与语义；本次新增授权不包含编译器实现。
- 用户随后明确同意语言设计方向，并要求先把语言设计完整再审阅；现已授权完整语法、语义、物理约束、测试规则与正反例设计。规范位于 `verimc/docs/languageSpec.md` 及其配套文件，初稿伪代码不作为正式语法；编译器实现仍另行安排。
- 用户批准后，在已批准范围内持续完成工作；常规、可逆的实现选择不需要反复确认。重大范围变更应更新设计并说明影响。
- 不自动启动子代理；只有用户或适用指令明确要求委派时才使用。

## 项目目标

- VeriMC 是面向 Minecraft 红石电路的硬件设计工具，长期包含：高性能方块级仿真与调试、VeriMC HDL 编译、真实 Minecraft 蓝图导出。
- 当前实现第一个模块：用户亲手搭建三维方块电路，在独立仿真器运行，并使用示波器等工具检查；第二模块的 VeriMC 语言同步进行设计审阅。
- HDL 编译器尚未获准实现；Litematic 导出及 Create（机械动力）蓝图导出属于后续模块。语言方案与 simulator 保持清晰的数据边界，不提前实现后续功能。
- 仿真性能是核心产品要求，必须同时保障机制正确性、可复现性和交互响应。
- 用户已确定仿真内核使用 **C++**，原因是便于本人理解和维护；不得重新引入 Rust 内核。当前设计建议 C++20 + CMake。
- 使用 **浏览器 + Three.js 界面**；本机原生 C++ 服务提供仿真与文件功能，浏览器负责搭建、渲染和波形。
- 用户已明确暂不实现 **TNT 复制机、流体农场、矿车计算机及依赖生物 AI 的机器**。不再把这些已确定的范围问题反复交给用户确认。
- 上述排除不意味着删除活塞、黏液块/蜂蜜块、漏斗、容器、铁轨供电或感测器本身；保留器件电路行为，复杂环境仅通过文档约定的输入/输出接口处理。

## Minecraft 兼容性

- 目标为 Minecraft **Java Edition 26.2 正式版**；不能把 Bedrock、快照或实验性规则混入目标。
- “26.2”是固定兼容版本，不自动随 Minecraft 最新版变化。必须记录实际参考的版本、构建校验值、规则设置和测试来源。
- 方块注册表、方块状态、更新顺序、计划刻、方块事件、随机行为与实体行为均需要明确的版本依据。
- 不把经验性红石知识、旧版资料或看起来合理的布尔逻辑当作 26.2 已验证行为。
- 已验证、未验证、仅支持外部刺激、未实现必须明确区分。缺少机制时不得用空气、无操作或固定输出静默替代，也不得宣称完整兼容。
- 环境抽象与全世界仿真的边界，以获批设计中的器件覆盖表和验收条件为准。

## 仿真与性能原则

- 仿真核心与编辑器、渲染、文件格式解耦；核心可以无界面运行。技术栈最终以获批设计为准。
- C++ 核心不依赖界面框架、HTTP/WebSocket 库、窗口系统或图形 API；本地服务是独立适配层。代码优先清晰的类、函数、标准容器和显式所有权；避免过度模板化和为每个方块建立多态对象。
- 使用 RAII、稳定索引与明确的缓冲区生命周期；调试检查越界、悬垂引用、未定义行为和数据竞争。单世界仿真线程独占可变状态，服务层发送有版本和序号的状态批次。
- 浏览器仅维护显示与编辑缓存，C++ 是仿真权威状态。仿真不依赖浏览器帧率或定时器；显示可合并，探针边沿、命令确认和断点不可静默丢弃。
- 通过分批二进制增量、订阅范围、接收确认及有限容量缓冲控制通信量；不能每个游戏刻发送完整世界，也不能假设 WebSocket 提供应用级背压。
- 从 Java 机制转写到 C++ 时，显式处理整数溢出、移位、负坐标除法/取模、随机源与有语义意义的容器迭代顺序；不能依赖 C++ 未定义行为或宿主平台偶然结果。
- 使用确定性的离散事件执行和局部依赖维护；避免每个游戏刻扫描整个世界、为每个方块分配通用对象、每个渲染帧复制整个场景。
- 不能为加速合并可观察的更新、随意重排事件、给红石粉添加虚构延迟，或用逻辑门替换具有更新顺序行为的方块电路。
- 每项缓存、去重、并行化、网络折叠及时间跳跃优化，都必须说明适用条件、失效条件和一致性证据。
- 先建立正确的参考执行与基准，再优化热点。性能指标必须来自 release 构建，附硬件、规模、活跃度、探针开销和参考版本。
- 禁止用空闲时间跳跃的 TPS 代表有实际活动电路的性能；禁止以未测量的速度倍数作为已实现结果。
- 仿真、渲染与采样频率独立；记录量有预算，历史截断可见。运行预算耗尽时暂停并保留状态，不丢事件后继续假装正确。

## 目录、命名和 Git

- 电路模拟子项目命名为 `simulator`；全部实现、构建配置、前端、测试、脚本、依赖、样例与实现进度记录放在仓库的 `simulator/` 内。根目录只保留仓库约定、项目索引和已有总设计文档。
- 语言子项目命名为 `verimc`；语言设计文档及以后获准的编译器源码、组件库、测试、样例和构建配置放在仓库的 `verimc/` 内，不混入 `simulator/`。
- 使用驼峰命名：变量、函数、方法、成员、命名空间和项目文件采用 lowerCamelCase；类、结构体和枚举类型采用 UpperCamelCase。标准工具要求的文件名（如 CMakeLists.txt）、Minecraft 原始 ID、第三方代码和既有外部协议字段保持规定形式。
- 使用 Git 管理仓库；每完成一个有意义且经过相应验证的大更新，单独提交。提交说明简明写清实现的功能或修复的问题，**完整提交说明不超过 20 个字/字符**。
- 只提交当前工作有关的源码、文档、依赖锁定与必要数据；构建产物、node_modules、下载缓存、参考游戏 JAR、用户工程和密钥不进入仓库。
- 不自动推送远程，不改写既有提交历史；未经明确授权不创建远程仓库。

## 验证与交付

- Minecraft 26.2 参考运行只用于机制研究和差分验证，不能成为日常独立仿真的必需依赖。
- 为新增行为建立有意义的器件测试、时序测试和必要的参考游戏差分场景。更新顺序、准连接、活塞、容器和随机机制需要专项验证。
- 记录随机源、输入时间线、放置历史及影响执行的队列状态；同一环境的重放应可定位到第一个差异事件。
- 浏览器界面是当前设计方向；将 C++ 编译到 WebAssembly 后完全在浏览器运行是另一项选择，尚未列入首版。若未来增加它，应复用同一 C++ 规则并重新验证性能与语义。
- 所有完成声明必须说明实际覆盖范围、验证结果和已知缺口；占位 UI 或仅静态展示不能算器件仿真完成。
- 项目文件必须保留 Minecraft 方块标识、状态与坐标语义，并区分电路设计与运行快照，为后续编译和导出提供稳定基础。
- 沟通和设计文档默认用中文；代码标识符使用清晰英文。不要把未来规划描述成已经存在的功能。

## 阅读 PDF、Office 文档与图片

- 理解 PDF、Office 等文档时，先使用 MinerU precise API 转成 Markdown，再阅读全文。不得以 `pdftotext` 作为主要读取方式。
- 使用 `python3 ~/.scanpdf/mineru_precise_to_markdown.py <input> -o <output.md> --language <language>`；也可传入公开 HTTP(S) URL。
- Token 仅由辅助脚本从 `MINERU_API_TOKEN` 或 `~/.scanpdf/api.txt` 读取；不得打印、引用、复制或提交 Token。
- 默认用 precise `vlm` 模型。公式密集文档解析不佳时，再与 `pipeline` 结果比较。
- 扫描 PDF 才加 `--ocr`；可直接提取文本的电子 PDF 不开启 OCR。保留表格和公式识别，英文文档使用 `--language en`。
- 普通图片直接通过多模态或图像查看能力阅读；只有用户明确要求图片转 Markdown 时才使用 MinerU，并按需启用 OCR。
- EPUB、MOBI、AZW、AZW3 先转成受支持的 PDF 或可靠 Markdown，再继续；不要把这些容器直接当作 precise API 支持格式。
- precise API 文档当前说明文件上限为 200 MB；实际页数及配额限制以实时文档或 API 响应为准。支持 `--page-ranges`，不要因旧轻量接口的 20 页限制拆分文档。
- Markdown 默认保存到原文件旁、使用相同文件名主干；保留 `<stem>_assets/` 引用。仅需要 JSON、布局或额外资料时才使用 `--keep-zip`。
- 已有转换先检查，只有明确要替换时才用 `--force`。分析前完整阅读 Markdown；图、公式、表格或阅读顺序有歧义时，再渲染核查原始页面。
- precise API 不可用、超额或拒绝文档时，明确报告失败。10 MB 且 20 页以内可回退到 `/Users/link/Documents/Codex/2026-08-03/https-mineru-net-apimanage-docs/outputs/mineru_to_markdown.py`；其余情况仅在必要时使用本地提取并说明局限。
